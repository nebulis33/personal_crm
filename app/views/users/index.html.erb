Hello <%= current_user.first_name %>
<%= link_to 'sign out', destroy_user_session_path, method: 'delete' %>

[upcoming events go here, this will be the left half]


[recent updates goes here, this will be the right half]