Hello <%= current_user.first_name %>
<%= link_to 'sign out', destroy_user_session_path, method: 'delete' %>
<br>
<%# left side %>
Upcoming Events:
<ul>
    <%= render partial: 'updates', locals: {events: @upcoming_events} %>
</ul>
<br>
<%# on the right side %>
Recent Events:
<ul>
    <%= render partial: 'updates', locals: {events: @recent_events} %>
</ul>
<br>
Alerts:
[people you haven't talked to in a while]
